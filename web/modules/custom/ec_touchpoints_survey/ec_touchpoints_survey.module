<?php

/**
 * @file
 * The module file with theming functions.
 */

/**
 * Implements hook_theme().
 */
function ec_touchpoints_survey_theme($existing, $type, $theme, $path) {
  return [
    'ec_touchpoints_survey' => [
      'variables' => [
        'id' => NULL,
        'file_name' => NULL,
      ],
      'template' => 'embedded-content/touchpoints-survey',
    ],
  ];
}

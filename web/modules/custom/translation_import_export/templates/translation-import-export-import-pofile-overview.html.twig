<p>Use the following button below to dowload the translated PO Package as a PO File.</p>
<p>Place the downloaded file within the codebase for discovery by Drupal for
importing.</p>

<a class="button" href="{{ path('translation_import_export.import_packages') }}">{{ 'Back to Imports'|t }}</a>
<a class="button" href="{{ path('translation_import_export.process_import_package', {'langcode':langcode, 'filename':url_filename}) }}">{{ 'Download Translated PO File'|t }}</a>

<h3>Translated Import File Preview</h3>

<textarea disabled="disabled" style="width:100%; height:500px;">
  {{ received_package|json_encode(constant('JSON_PRETTY_PRINT'))|raw }}
</textarea>

{#
/**
 * @file
 * Default theme implementation to display a block content: Site-wide alert.
 */
#}

{# Caching the data in content object #}
{% set savedCache = content | render %}

{% include '@uswds_templates/usa-site-alert.html.twig' with {
  'alert_type': content.field_alert_type | field_value | render,
  'text': content.field_alert_content | field_value
} %}
